const DefaultLayout = ({ children }: { children: React.ReactNode }) => (
  <main className="px-5 py-3 md:px-12 md:py-16 max-w-screen-2xl m-auto relative text-black">{children}</main>
);

export default DefaultLayout;
